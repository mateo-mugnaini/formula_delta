# Testing Agent Instructions

Testing work owns deterministic unit tests, integration tests, fixtures,
replay validation, protocol regression tests, and failure scenarios.

Tests must not depend primarily on live Formula 1 availability.
