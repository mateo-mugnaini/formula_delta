# UX/UI Agent Instructions

This area owns design tokens, typography, spacing, density, accessibility,
semantic race states, stable live layouts, and interaction consistency.

Presentation decisions must remain separate from the normalized domain model.
